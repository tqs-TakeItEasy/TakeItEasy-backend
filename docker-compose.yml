version: "3.9"

# volumes:
#   tie_data:

services:
  # mysql_tie:
  #   image: mysql/mysql-server:8.0
  #   container_name: tie_mysql
  #   command: mysqld
  #   volumes:
  #     - tie_data:/var/lib/mysql
  #   ports:
  #     - "3306:3306"
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=root
  #     - MYSQL_DATABASE=tie_db
  #     - MYSQL_USER=tie
  #     - MYSQL_PASSWORD=tiepasswd
  #   networks:
  #     - tie_network
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-P", "3306", "-u", "root", "-proot"]
  #     interval: 10s
  #     timeout: 10s
  #     retries: 5

  spring_tie:
    # depends_on:
      # mysql_tie:
        # condition: service_healthy
    build: ./backend
    container_name: tie_api
    restart: unless-stopped
    ports:
      - 8080:8080